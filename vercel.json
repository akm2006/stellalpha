{
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"dev\" ]; then exit 0; fi; git diff HEAD^ HEAD --quiet -- . ':(exclude)README.md' ':(exclude)ai/' ':(exclude)task.md'",
  "crons": [
    {
      "path": "/api/cron/cleanup-trades",
      "schedule": "0 0 * * *"
    }
  ]
}